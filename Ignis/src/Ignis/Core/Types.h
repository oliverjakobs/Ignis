#pragma once

#include <string>
#include <map>
#include <vector>
#include <glm/glm.hpp>

using std::vector;

typedef glm::vec4 color;

typedef unsigned char byte;
typedef unsigned int uint;

typedef std::vector<glm::vec2> vertices2;
typedef std::vector<glm::vec3> vertices3;
typedef std::vector<glm::vec4> vertices4;
